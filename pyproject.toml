[project]
name = "mcp-server"
version = "0.0.1"
description = "Serveur MCP personnel - roadmap pre-implementation"
authors = [
  { name = "Peter Ufens", email = "269768127+Peter-Ufens@users.noreply.github.com" },
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = []
